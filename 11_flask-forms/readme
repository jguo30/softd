# MyJuicyWork - William Guo, Jacob Guo, Mahir Riki
# SoftDev
# K11: Form(s) Like Voltron
# Oct 14th 2022
# Time spent: 0.6 hours

'''
Predictions:
print(app) - prints URL of the root
print(request) - prints the webpage that it's serving on
print(request.args) - prints  user input in the termina

Disco:
print(request.headers) - information on the console, browser using 
when you inputed username, app route changes to /auth

when commented out print(request.username), got badrequestkeyerror the the route was '/'
when route was '/auth', printed out the username that uper inputed into consol

<"form action = "/auth"> - connects the front end to back
'''
